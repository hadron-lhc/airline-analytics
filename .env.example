# Conexión a PostgreSQL para cargar y analizar la simulación.
# Copia este archivo a `.env` y rellena tus valores.
#   cp .env.example .env
# El archivo `.env` NO se commitea (ver .gitignore).

DB_NAME=airline
DB_USER=postgres
DB_PASSWORD=tu_contraseña
DB_HOST=localhost
DB_PORT=5432
